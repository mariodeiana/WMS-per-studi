const p=new URLSearchParams(location.search),operator=p.get("operator"),practice=p.get("practice"),task=p.get("task");
document.querySelector("#identity").textContent=`OPERATORE · ${operator}`;
document.querySelector("#back").href=`/queue.html?operator=${encodeURIComponent(operator)}`;
const box=document.querySelector("#message");
const selectedAttachments=[];
function fail(error){box.textContent=error.message;box.hidden=false;box.scrollIntoView({block:"nearest"})}
function escapeHtml(value){const node=document.createElement("span");node.textContent=value??"";return node.innerHTML}
function evidenceHtml(item){const meta=[item.document_type,item.description,item.actor,item.created_at?new Date(item.created_at).toLocaleString("it-IT"):""].filter(Boolean).join(" · ");const type=String(item.content_type||"").toLowerCase();const canInline=type.startsWith("image/")||type==="application/pdf"||type.startsWith("text/");if(!canInline)return `<span class="doc-preview"><a class="doc-icon" href="${item.preview_url}" target="_blank" title="Apri documento · ${escapeHtml(meta)}">📄 ${escapeHtml(item.filename)}</a><span class="doc-popover"><small>${escapeHtml(meta)}</small><span>Anteprima non disponibile nel browser.</span><a href="${item.preview_url}" target="_blank">Apri documento</a></span></span>`;const preview=type.startsWith("image/")?`<img src="${item.preview_url}" alt="Anteprima ${escapeHtml(item.filename)}">`:`<iframe src="${item.preview_url}" title="Anteprima ${escapeHtml(item.filename)}"></iframe>`;return `<span class="doc-preview"><a class="doc-icon" href="${item.preview_url}" target="_blank" title="Apri anteprima · ${escapeHtml(meta)}">📄 ${escapeHtml(item.filename)}</a><span class="doc-popover">${preview}<small>${escapeHtml(meta)}</small><a href="${item.preview_url}" target="_blank">Apri anteprima</a></span></span>`}
function resultHtml(result){const docs=(result.evidence||[]).map(evidenceHtml).join("");const taskLabel=[result.related_task_code||result.action,result.related_task_title].filter(Boolean).join(" · ");return `<li><strong>${escapeHtml(taskLabel)} · ${escapeHtml(result.outcome)}</strong>${result.note?`<br>${escapeHtml(result.note)}`:""}<br><small>${escapeHtml(result.actor)} · ${result.timestamp?new Date(result.timestamp).toLocaleString("it-IT"):""}</small>${docs?`<div class="evidence-list">${docs}</div>`:""}</li>`}
function journalHtml(entry){const docs=(entry.evidence||[]).map(evidenceHtml).join("");const kind=entry.type==='REOPENED'?` · <strong>RIAPERTO</strong>`:"";return `<div class="result"><small>${entry.at?new Date(entry.at).toLocaleString("it-IT"):""} · ${escapeHtml(entry.actor)}${kind}</small>${entry.note?`<p>${escapeHtml(entry.note)}</p>`:""}${docs?`<div class="evidence-list">${docs}</div>`:""}</div>`}
async function load(){const response=await fetch(`/api/tasks/${practice}/${task}?operator=${encodeURIComponent(operator)}&context=1`),data=await response.json();if(!response.ok)throw new Error(data.error);const history=(data.previous_results||[]).map(resultHtml).join("");const reopened=data.task.reopen_reason?`<section class="reopen-notice"><p class="eyebrow">Task riaperto dal manager</p><strong>Motivazione</strong><p>${escapeHtml(data.task.reopen_reason)}</p></section>`:"";const diary=(data.task_journal||[]).length?`<section class="context"><h2>Diario del task</h2>${data.task_journal.map(journalHtml).join("")}</section>`:"";document.querySelector("#detail").innerHTML=`<p class="eyebrow">${data.practice.type} · ${data.practice.id}</p><h1>${escapeHtml(data.task.title)}</h1><p>Cliente <strong>${escapeHtml(data.practice.client_id)}</strong> · periodo ${data.practice.period_start} / ${data.practice.period_end} · scadenza ${data.practice.due_date}</p><p>Stato: <strong>${data.task.status==='IN_LAVORAZIONE'?'IN CORSO':data.task.status}</strong> · Dipendenze esplicite: <strong>${data.task.depends_on.join(', ')||'nessuna'}</strong></p>${reopened}<section class="instructions"><p class="eyebrow">Istruzioni operative</p><h2>Cosa devi fare</h2><p>${escapeHtml(data.task.instructions||"Nessuna istruzione definita.")}</p></section>${diary}${history?`<section class="context"><h2>Materiale e risultati precedenti</h2><ul>${history}</ul></section>`:""}<form id="result-form"><label>Esito<select id="outcome" required><option value="" selected>Seleziona esito…</option><option value="POSITIVO">Positivo</option><option value="CON_RILIEVI">Con rilievi</option></select></label><label>Nota<textarea id="note" rows="4" placeholder="Aggiungi una nota di lavoro o il risultato finale"></textarea></label><fieldset class="attachments-fieldset"><legend>Evidenze</legend><input id="attachment-picker" class="file-picker-hidden" type="file" multiple><div id="attachment-meta"></div><small>Gli allegati vengono salvati anche durante il lavoro e restano collegati al task. Massimo 5 MB per documento.</small></fieldset><div class="task-actions"><button id="pause" type="button" class="secondary">Salva e torna ai compiti</button><button id="complete" type="button" ${data.task.status==='COMPLETATO'?'disabled':''}>${data.task.status==='COMPLETATO'?'Completata':'Registra risultato e completa'}</button></div></form>`;const picker=document.querySelector("#attachment-picker");picker.addEventListener("change",()=>{addSelectedFiles([...picker.files]);picker.value=""});document.querySelector("#result-form").addEventListener("submit",event=>event.preventDefault());document.querySelector("#result-form").addEventListener("keydown",event=>{if(event.key==="Enter"&&event.target.tagName!=="TEXTAREA")event.preventDefault()});document.querySelector("#pause").addEventListener("click",()=>saveProgress().catch(fail));document.querySelector("#complete").addEventListener("click",()=>complete().catch(fail));renderAttachmentMeta()}
function addSelectedFiles(files){for(const file of files){if(file.size>5*1024*1024){fail(new Error(`${file.name}: dimensione superiore a 5 MB`));continue}selectedAttachments.push({file,description:"",documentType:""})}renderAttachmentMeta()}
function renderAttachmentMeta(){const container=document.querySelector("#attachment-meta");const rows=selectedAttachments.map((item,i)=>`<div class="attachment-row"><div class="attachment-file">📄 ${escapeHtml(item.file.name)}</div><input aria-label="Descrizione ${escapeHtml(item.file.name)}" data-description="${i}" type="text" value="${escapeHtml(item.description)}" placeholder="Descrizione"><input aria-label="Tipo documento ${escapeHtml(item.file.name)}" data-document-type="${i}" type="text" value="${escapeHtml(item.documentType)}" placeholder="Tipo documento"><button type="button" class="icon-button" data-remove="${i}" title="Elimina l'allegato" aria-label="Elimina l'allegato ${escapeHtml(item.file.name)}">−</button></div>`).join("");const addRow=`<div class="attachment-row attachment-add-row"><button type="button" class="icon-button" id="add-attachment-row" title="Aggiungi allegato" aria-label="Aggiungi allegato">+</button><span></span><span></span><span></span></div>`;container.innerHTML=`<div class="attachment-table"><div class="attachment-head"><span>Documento</span><span>Descrizione</span><span>Tipo documento</span><span></span></div>${rows}${addRow}</div>`;container.querySelectorAll("[data-description]").forEach(input=>input.addEventListener("input",()=>{selectedAttachments[Number(input.dataset.description)].description=input.value}));container.querySelectorAll("[data-document-type]").forEach(input=>input.addEventListener("input",()=>{selectedAttachments[Number(input.dataset.documentType)].documentType=input.value}));container.querySelectorAll("[data-remove]").forEach(button=>button.addEventListener("click",()=>{selectedAttachments.splice(Number(button.dataset.remove),1);renderAttachmentMeta()}));document.querySelector("#add-attachment-row").addEventListener("click",()=>document.querySelector("#attachment-picker").click())}
function readFile(file){return new Promise((resolve,reject)=>{if(file.size>5*1024*1024){reject(new Error(`${file.name}: dimensione superiore a 5 MB`));return}const reader=new FileReader();reader.onload=()=>resolve(String(reader.result).split(",",2)[1]||"");reader.onerror=()=>reject(new Error(`Impossibile leggere ${file.name}`));reader.readAsDataURL(file)})}
async function buildAttachments(){const attachments=[];for(const item of selectedAttachments)attachments.push({filename:item.file.name,content_type:item.file.type||"application/octet-stream",description:item.description,document_type:item.documentType||"DOCUMENTO",content_base64:await readFile(item.file)});return attachments}
async function saveProgress(){box.hidden=true;const attachments=await buildAttachments();const response=await fetch(`/api/practices/${practice}/tasks/${task}/progress`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({actor:operator,note:document.querySelector("#note").value,attachments})}),data=await response.json();if(!response.ok)throw new Error(data.error);location.href=document.querySelector("#back").href}
async function complete(){box.hidden=true;const outcome=document.querySelector("#outcome").value;if(!outcome){fail(new Error("Seleziona l'esito prima di completare il task."));document.querySelector("#outcome").focus();return}const attachments=await buildAttachments();const response=await fetch(`/api/practices/${practice}/tasks/${task}/complete`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({actor:operator,outcome,note:document.querySelector("#note").value,attachments})}),data=await response.json();if(!response.ok)throw new Error(data.error);location.href=document.querySelector("#back").href}
load().catch(fail);
