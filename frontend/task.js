const p=new URLSearchParams(location.search),operator=p.get("operator"),practice=p.get("practice"),task=p.get("task");document.querySelector("#identity").textContent=`OPERATORE · ${operator}`;document.querySelector("#back").href=`/queue.html?operator=${encodeURIComponent(operator)}`;
const box=document.querySelector("#message");function fail(error){box.textContent=error.message;box.hidden=false}function escapeHtml(value){const node=document.createElement("span");node.textContent=value??"";return node.innerHTML}async function load(){const response=await fetch(`/api/tasks/${practice}/${task}?operator=${encodeURIComponent(operator)}&context=1`),data=await response.json();if(!response.ok)throw new Error(data.error);const history=data.previous_results.map(result=>`<li><strong>${escapeHtml(result.related_task_code||result.action)}</strong> — ${escapeHtml(result.outcome)}${result.note?`<br>${escapeHtml(result.note)}`:""}</li>`).join("");document.querySelector("#detail").innerHTML=`<p class="eyebrow">${data.practice.type} · ${data.practice.id}</p><h1>${escapeHtml(data.task.title)}</h1><p>Cliente <strong>${escapeHtml(data.practice.client_id)}</strong> · periodo ${data.practice.period_start} / ${data.practice.period_end} · scadenza ${data.practice.due_date}</p><p>Dipendenze esplicite: <strong>${data.task.depends_on.join(', ')||'nessuna'}</strong></p>${history?`<section class="context"><h2>Risultati precedenti</h2><ul>${history}</ul></section>`:""}<form id="result-form"><label>Esito<select id="outcome"><option value="POSITIVO">Positivo</option><option value="CON_RILIEVI">Con rilievi</option></select></label><label>Nota<textarea id="note" rows="4" placeholder="Descrivi il risultato del lavoro"></textarea></label><label>Evidenze <input id="attachments" type="file" multiple></label><small>I file sono registrati nel fascicolo come metadati in questa versione dimostrativa.</small><button id="complete" ${data.task.status==='COMPLETATO'?'disabled':''}>${data.task.status==='COMPLETATO'?'Completata':'Registra risultato e completa'}</button></form>`;document.querySelector("#result-form").onsubmit=event=>{event.preventDefault();complete().catch(fail)}}async function complete(){const attachments=[...document.querySelector("#attachments").files].map(file=>({filename:file.name,content_type:file.type||"application/octet-stream"}));const response=await fetch(`/api/practices/${practice}/tasks/${task}/complete`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({actor:operator,outcome:document.querySelector("#outcome").value,note:document.querySelector("#note").value,attachments})}),data=await response.json();if(!response.ok)throw new Error(data.error);location.href=document.querySelector("#back").href}load().catch(fail);
